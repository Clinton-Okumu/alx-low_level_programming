#include <stdio.h>
/**
 * main - function to print base 10 numbers
 * Return: return (0) success
 */
int main(void)
{
	int i;

	for (i = 0; i < 9; i++)
{
	printf("%d", i);
}
printf("\n");
return (0);
}
